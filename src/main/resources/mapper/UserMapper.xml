<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.buba.farm.mapper.UserMapper">
   <!--  User login(String tel);
   /*用户登录*/-->
    <select id="login" resultType="com.buba.farm.pojo.SecUser">
        SELECT * FROM sec_user WHERE tel = #{tel}
    </select>

</mapper>
